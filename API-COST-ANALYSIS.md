# API 비용 분석 · 에이전트 유형별 실측과 최적화 방향

> 적용하지 않은 분석 문서다. 2026-08-16 에 세 샘플을 실제로 돌려 얻은 호출 수를 기준으로 쓴다.
> 이미지·3D 단가는 코드의 상수(`imageEngines.ts`, 3D 는 공급사 고시)이고, **추론 토큰은 실측이 없다** —
> 서버가 응답의 `usage` 를 어디에도 남기지 않기 때문이다. 그 부분은 공개 단가와 프롬프트 길이로 추정했고,
> 추정임을 표시했다. 이게 이 문서의 첫 번째 권고와 연결된다 (§4-①).

---

## 1. 무엇이 API 를 부르는가

파이프라인의 지출 지점은 여섯 종류다. 에이전트 유형이 다르면 S1 이 달라지고, S2 이후는 설정값이 결정한다.

| 지출 지점 | 단계 | 호출 대상 | 무엇을 하나 |
|---|---|---|---|
| **A. 웹 조사** | S1 | 추론 모델 + `web_search` 도구, `reasoning: high` | 신호·경쟁사·리테일 펄스·트렌드 리포트·시즌 도시에. 검색 1회마다 과금 |
| **B. 문서 읽기** | S1 | 추론 모델 (비전 입력) | 무드보드 PDF 페이지, 시리즈 아카이브 사진 |
| **C. 설계 저작** | S1b·S2·S3 | 추론 모델 | 영토 계획(`high`) · 게놈(`medium`) · 컨셉(`medium`) |
| **D. 이미지** | S2·S3·S4 | 이미지 모델 (생성/편집) | 스케치·아웃솔 시트·렌더·뷰·컬러웨이·컨셉·캠페인 컷 |
| **E. 비전 검증** | S3 | 추론 모델 (비전, `low`) | 렌더가 스펙과 맞는지 확인, 수리 후 재확인 |
| **F. 3D** | S5 | 3D 생성 서비스 | 최종 픽마다 GLB 하나 |
| G. MD 리뷰 | S4 | 추론 모델 (`high`) | 페르소나가 후보 전체를 평가 · Run 당 1회 |

이미지·3D 는 **건당 정액**이라 계산이 정확하다. 조사·저작·검증은 **토큰 과금**이라 프롬프트 길이와 추론 깊이에 따라 흔들린다.

---

## 2. 에이전트 유형별 실측 (2026-08-16 세 샘플)

세 Run 은 이미지 상한 48 · detail 엔진 · 렌더 50% · 뷰 3 · 컬러웨이 2 · 캠페인 4 · 3D 켬 으로 거의 같은 설정이고, 다른 것은 **모드**와 스케치 수·컨셉 수뿐이다. 그래서 모드 간 차이가 그대로 보인다.

### 2-1. 호출 수

| | 트렌드 (러닝화) | 시리즈 (조던 → 코트하이) | 무드보드 (MICAM) |
|---|---|---|---|
| 스케치 수 / 스케치당 컨셉 | 8 / 3 | 6 / 4 | 8 / 3 |
| **웹 검색 (A)** | **388** | 270 | **0** |
| ├ 트렌드 신호 | 107 | 24 | — |
| ├ 시즌 도시에 | 179 | 168 | — |
| ├ 트렌드 리포트 | 102 | 78 | — |
| └ 경쟁사·펄스 | 87 + 31 (캐시 재사용) | — | — |
| 문서/사진 읽기 (B) | — | 아카이브 7장 ×1 | PDF 14쪽 ×1 |
| 저작 호출 (C) | 영토 1 + 게놈 8 + 컨셉 4 | 영토 1 + 게놈 6 + 컨셉 3 | 영토 1 + 게놈 8 + 컨셉 4 |
| **이미지 (D)** | **48** (생성 9 · 편집 39) | **38** (생성 8 · 편집 30) | **44** (생성 10 · 편집 34) |
| 비전 검증 (E) | 7 | 4 | 6 |
| MD 리뷰 (G) | 1 | 1 | 1 |
| **3D (F)** | **3** | 2 | 2 |
| 소요 시간 | 100분 | 108분 | 103분 |

트렌드가 388회 검색을 한 것에 비해 시리즈가 270회인 이유는 시리즈 모드가 경쟁사 조사를 하지 않아서다. 무드보드는 외부 조사가 없으므로 검색 0.

### 2-2. 비용 추정 (USD)

단가 근거: 이미지 detail 0.190/장, fast 0.045/장 (`imageEngines.ts`). 3D 는 공급사 고시 기준 건당 약 0.20~0.40 로 잡았다. 조사·저작 토큰은 공개 단가와 프롬프트 규모로 **추정**이다 — 실측이 없으니 ±40% 로 봐야 한다.

| | 트렌드 | 시리즈 | 무드보드 |
|---|---|---|---|
| A. 웹 조사 (검색 + 추론 토큰) | **~$4.5–6.5** | ~$3.0–4.5 | $0 |
| B. 문서/사진 읽기 | — | ~$0.15 | ~$0.40 (14쪽 비전) |
| C. 설계 저작 (영토·게놈·컨셉) | ~$0.60 | ~$0.50 | ~$0.60 |
| **D. 이미지 (detail 0.19)** | **$9.12** | **$7.22** | **$8.36** |
| E. 비전 검증 | ~$0.15 | ~$0.10 | ~$0.15 |
| F. 3D | ~$0.60–1.20 | ~$0.40–0.80 | ~$0.40–0.80 |
| G. MD 리뷰 | ~$0.10 | ~$0.10 | ~$0.10 |
| **Run 합계 (추정)** | **~$15–18** | **~$11.5–13.5** | **~$10–10.5** |

**비율로 보면 명확하다.** 세 Run 모두 **이미지가 55–80%**, 트렌드에서는 **웹 조사가 30–35%**. 나머지 다섯 지점을 다 합쳐도 10% 미만이다. 최적화는 이미지와 조사에서만 의미가 있다.

### 2-3. 설정값 하나가 얼마를 움직이나

같은 모드에서 설정을 바꿀 때 이미지 장수가 어떻게 변하는지. 파이프라인 예산 모델(`pipeline.ts` `budget`, `perDesignExtras`)을 그대로 계산했다.

| 설정 | 값 | 이미지 장수 변화 | detail 기준 비용 변화 |
|---|---|---|---|
| `imageEngine` | detail → fast | 장수 동일 | **–76%** (0.19 → 0.045) |
| `imageBudget` | 48 → 24 | 상한이 절반 · 실제로는 컨셉·컬러웨이가 먼저 잘림 | 약 –50% |
| `renderRatio` | 0.5 → 0.25 | 렌더 대상이 절반 → S3 이후 전부 절반 | 약 –40% |
| `designsPerSketch` | 3 → 1 | 디자인당 컨셉 렌더 –2 | 디자인당 –$0.38 |
| `colorwayCount` | 2 → 0 | 디자인당 –2 | 디자인당 –$0.38 |
| `viewCount` | 3 → 1 | 디자인당 –2 | 디자인당 –$0.38 |
| `campaignShots` | 4 → 0 | 픽당 –4 | 픽당 –$0.76 |
| `make3d` | true → false | 3D 0 | –$0.4~1.2 |
| `sketchCount` | 12 → 6 | 스케치·아웃솔 시트 절반, 렌더 대상도 절반 | 약 –45% |
| 조사 깊이 (`depth`, 하위 질문 수) | 트렌드에서 검색 ~100회 ↔ 도시에 ~180회 | — | 검색 –30% 시 약 –$1.5 |

**가장 큰 레버 하나는 이미지 엔진이다.** fast 로 바꾸면 같은 장수에서 이미지 비용이 $9 → $2.2 로 떨어진다. 트렌드 Run 전체로 보면 $15–18 → $8–11.

---

## 3. 어디서 새는가 · 성능을 안 깎고 줄일 수 있는 것

### 3-1. 이미지 (가장 크다)

**① 엔진을 단계별로 나눈다.** 지금은 Run 전체가 하나의 엔진이다. 그런데 단계마다 필요한 품질이 다르다.

| 단계 | 지금 | 필요한 것 | 제안 |
|---|---|---|---|
| S2 스케치·아웃솔 시트 | detail | 흑백 선화. 형태만 맞으면 됨 | **fast** — 선화는 detail 의 질감 표현이 쓰일 데가 없다 |
| S3 기준 렌더 | detail | 보드의 얼굴. 최고 품질 | detail 유지 |
| S3 추가 뷰·컬러웨이 | detail | 같은 제품의 다른 각도/색 | **fast** — 편집이라 형태는 기준 렌더가 잡아 준다 |
| S3 컨셉 렌더 | detail | 소재·색 변주. 질감이 중요 | detail 유지 (material_shift 는 질감이 곧 내용) |
| S4 캠페인 컷 | detail | 착용·연출. 분위기 | **fast** 로 충분한 경우가 많다 |

트렌드 Run 48장 기준: 스케치 9 + 뷰·컬러웨이 ~16 + 캠페인 12 = **37장을 fast 로** 돌리면 $9.12 → $2.09 + $1.67 = **$3.76**. 이미지만 –59%, Run 전체 –35%. 보드에서 사람이 보는 히어로와 컨셉은 그대로다.

**② 예산이 먼저 잘리는 곳을 뒤집는다.** 지금 `perDesignExtras` 는 컨셉 → 필수 뷰 → 컬러웨이 순으로 소비한다 (컨셉이 먼저). 상한이 낮으면 컬러웨이가 0 이 된다 — 옛 샘플이 전부 그랬다. 컬러웨이는 편집이라 싸고 (fast 면 0.045), 컨셉은 새 소재를 그리므로 detail 이 필요하다. **싼 것부터 채우고 비싼 것을 마지막에** 두면 같은 예산에서 카드가 더 채워진다.

**③ 서버 캐시는 이미 있다.** 같은 프롬프트는 재과금 없이 재사용된다 (`server/openai-api.mjs` 해시 캐시). 재시도가 두 번 물지 않는 이유다. 다만 프롬프트에 브랜드 팔레트 hex 가 들어가므로 브랜드를 조금만 바꿔도 캐시가 갈린다 — 이건 맞는 동작이다.

### 3-2. 웹 조사 (트렌드에서 두 번째로 크다)

**④ 시즌 도시에가 가장 무겁다 (179회).** 매크로 4개를 병렬로 각각 깊게 조사하기 때문이다. 그런데 도시에는 **품목·시즌·시장이 같으면 브랜드가 달라도 같은 답**이다 (캐시 키에 브랜드가 없다 — 맞는 설계). 즉 같은 회사가 러닝화 FW26 KR 로 Run 을 세 번 돌리면 도시에는 **한 번만 과금**된다. 실측에서 시리즈 Run 의 도시에 168회는 트렌드와 품목이 달라서(코트하이) 새로 돈 것이다.

→ 운영에서는 **품목×시즌×시장 조합이 몇 개인지**가 조사 비용을 결정한다. 열 브랜드가 같은 품목을 조사하면 조사비는 1/10 이다.

**⑤ 하위 질문 셋(바텀·SNS·크로스카테고리)은 항상 돈다.** 이건 이번에 넣은 것이고 신호의 `part`·`social_platforms` 를 채우는 근거라 빼면 안 된다. 다만 `depth` 앞의 계획된 질문 수는 줄일 수 있다.

**⑥ `reasoning: high` 가 조사 전 구간에 걸려 있다.** 조사는 검색 결과를 읽고 정리하는 일이라 `medium` 으로도 스키마를 채운다. 추론 토큰이 검색 비용과 비슷한 규모일 가능성이 있는데 **실측이 없어 확인이 안 된다.** 이건 §4-① 없이는 판단 못 한다.

### 3-3. 그 외

**⑦ 비전 검증은 이미 `low`** 이고 건당 싸다. 건드릴 것 없음.
**⑧ MD 리뷰는 Run 당 1회** 이고 페르소나·브랜드·후보 스펙으로 캐시된다. 건드릴 것 없음.
**⑨ 3D 는 픽당 정액**이고 이미 topN 으로 제한된다. `make3d` 는 이미 옵션이다.

---

## 4. 오픈소스·자체 개발로 대체할 수 있는가

성능을 **유지**한다는 조건에서, 대체 가능성을 지출 지점별로 본다.

| 지출 지점 | 대체 후보 | 성능 유지 여부 | 판단 |
|---|---|---|---|
| **D. 이미지 — 스케치·아웃솔 시트** | 오픈 웨이트 확산 모델 (SDXL/FLUX 계열) + LoRA | **가능**. 흑백 선화는 스타일이 좁아 파인튜닝이 잘 먹는다. 우리 스케치 300장이면 LoRA 학습 가능 | **가장 먼저 시도할 것**. Run 당 ~10장 → 자체 GPU 면 한계비용 0 |
| D. 이미지 — 편집 (뷰·컬러웨이) | 오픈 이미지 편집 모델 (인페인팅/img2img) | 조건부. "같은 제품의 다른 각도"는 형태 유지가 어렵다. 컬러웨이(색만 바꿈)는 잘 된다 | 컬러웨이만 먼저 |
| D. 이미지 — 기준 렌더·컨셉 | 오픈 모델 | 아직 어렵다. 소재 질감·구조 정합성이 상용 대비 떨어지고, 비전 검증 실패율이 오르면 수리 비용이 상쇄한다 | 유지 |
| **A. 웹 조사** | 검색 API + 자체 스크래핑 + 오픈 LLM 정리 | 부분 가능. 검색은 저가 API 로 대체되지만, "확인한 사실만, 출처 URL 과 함께, 스키마 강제" 를 지키는 정리는 상용 추론 모델이 훨씬 안정적이다 | 검색만 분리, 정리는 유지 |
| C. 저작 (게놈·컨셉) | 오픈 LLM | 조건부. 스키마 준수와 한국어 품질이 관건. 게놈은 7파트 × form/material 을 빠짐없이 채워야 하는데 오픈 모델은 스키마 위반이 잦다 | 스키마 재시도 루프를 붙이면 시도해 볼 만하나 지금은 지출이 작아 우선순위 낮음 |
| E. 비전 검증 | 오픈 VLM | 가능성 있음. 검사 항목이 고정(토 형태·힐 높이·클로저)이라 좁은 태스크 | 지출이 작아 우선순위 낮음 |
| B. 문서 읽기 | 오픈 VLM + PDF 파서 | 가능. 14쪽 읽기는 상용도 싸다 | 우선순위 낮음 |
| F. 3D | 오픈 image-to-3D | 품질 격차가 크다. 지금도 GLB 가 12–15MB 로 무겁고 형태 정확도가 관건 | 유지 |
| G. MD 리뷰 | — | Run 당 1회. 대체 이득 없음 | 유지 |

**요약: 자체 개발이 실제로 돈이 되는 자리는 하나다 — 스케치·아웃솔 시트.** 선화는 상용 모델의 강점(질감·조명)을 쓰지 않는데 같은 단가를 낸다. Run 당 ~10장, 연 1,000 Run 이면 10,000장 = detail 기준 **$1,900/년**, fast 기준 $450/년. 자체 GPU 한 대와 LoRA 한 번이면 그 뒤로는 0 이다. 나머지는 상용 유지가 성능 대비 맞다.

---

## 5. 권고 순서

> **정정 (2026-08-16).** 아래 ② 「단계별 엔진 분리 –35%」는 실측으로 반박됐다.
> fast 편집은 기준 렌더의 형태를 유지하지 못해 "모든 컷이 같은 제품" 보장을 깬다.
> 안전한 절감폭은 –35% 가 아니라 약 –7% 다. 근거와 정정된 권고는
> [API-COST-DETAIL.md](API-COST-DETAIL.md) §8 을 볼 것.

적용 전에 순서와 기대치를 적어 둔다. 앞의 것이 싸고 확실하다.

**① 토큰 사용량을 기록한다 (선행 조건).**
서버의 `ask()` 세 곳(`design-api`, `research-api`, `upload-api`)에서 응답의 `usage` 를 캐시 파일과 로그에 남긴다. 지금은 조사·저작의 실제 비용을 **아무도 모른다**. 이 문서의 A·C·E 추정치가 ±40% 인 이유다. 반나절 작업이고, 이게 없으면 ②~⑤ 의 효과를 측정할 수 없다.

**② 단계별 엔진 분리.** `imageEngine` 하나를 `{ sketch, render, edit, campaign }` 넷으로 나눈다. 기본값을 sketch=fast, edit=fast, campaign=fast, render=detail 로. 트렌드 Run 기준 –35%, 보드의 히어로·컨셉은 그대로. 위저드에 노출할지 말지는 별개.

**③ 예산 소비 순서 뒤집기.** 컬러웨이 → 필수 뷰 → 컨셉 순으로. 같은 상한에서 카드가 더 채워진다.

**④ 조사 `reasoning: high` → `medium` 실험.** ① 이 있어야 효과를 잰다. 스키마 준수율이 안 떨어지면 유지.

**⑤ 스케치 LoRA 검토.** 우리 스케치 데이터가 300장 넘으면 착수. 그 전까지는 ② 의 fast 로 충분하다.

지금 하지 말 것: 기준 렌더·컨셉·3D 를 오픈소스로 바꾸는 것. 비전 검증 실패율이 오르면 수리 루프가 비용을 되돌려 놓고, 보드 품질이 곧 이 제품이다.

---

## 6. 계산 근거

- 호출 수: `.cache/run-sample.log` `.cache/run-series.log` `.cache/run-mood.log` 의 로그와 `src/samples/*.json` 의 `images[].origin` 에서 셌다.
- 이미지 단가: `src/core/imageEngines.ts` — fast 0.045 / detail 0.190 (1024²).
- 예산 모델: `src/core/pipeline.ts` 의 `budget`, `sketchCap = imageBudget × 0.4`, `perDesignExtras = floor((left − advancing) / advancing)`.
- 조사 캐시 키: `server/research-api.mjs` (`trend11ft`, `dossier7ft` 등) — 브랜드는 도시에 키에 없고, 시장·품목·시즌·라인 해시가 들어간다.
- 추론 단가: 공개 가격표 기준 추정. **실측 아님.**
