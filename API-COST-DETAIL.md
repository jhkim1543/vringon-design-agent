# API 세부 내역 · 엔드포인트별 · 기능별 비용

> [API-COST-ANALYSIS.md](API-COST-ANALYSIS.md) 의 세부판. 총액이 아니라 **어느 기능이 어느 API 를 몇 번 부르고 얼마를 쓰는지**를 적는다.
> 호출 수는 2026-08-16 세 샘플의 실측이다. 이미지·3D 단가는 정액이라 정확하고, 추론 토큰은 추정이다
> (서버가 `usage` 를 기록하지 않는다 — 이게 1번 권고인 이유).

---

## 1. 엔드포인트 전체 목록 · 누가 부르는가

서버는 26개 경로를 연다. 그중 **과금되는 것은 12개**다. 나머지는 파일 서빙·프로브·업로드로 공짜다.

### 1-1. 과금 엔드포인트

| 엔드포인트 | 뒷단 | effort | 부르는 기능 | 모드 |
|---|---|---|---|---|
| `/api/research/trends` | 추론 + `web_search` | high | 트렌드 신호 + 하위 질문 분해 + 트렌드 리포트 | 트렌드 · 시리즈 |
| `/api/research/dossier` | 추론 + `web_search` | high | 시즌 도시에 (매크로 4개 병렬 + 연도별 맥락) | 트렌드 · 시리즈 |
| `/api/research/competitors` | 추론 + `web_search` | high | 경쟁사 제품 조사 (브랜드마다 1콜) | **트렌드만** |
| `/api/research/pulse` | 추론 + `web_search` | high | 백화점·리테일 베스트셀러 펄스 | **트렌드만** |
| `/api/analyze/series` | 추론 (비전) | high | 아카이브 사진에서 DNA 읽기 | **시리즈만** |
| `/api/analyze/moodboard` | 추론 (비전) | high | 업로드 PDF 읽기 | **무드보드만** |
| `/api/design/territories` | 추론 | **high** | 설계 영토 계획 (Run 당 1회) | 전 모드 |
| `/api/design/genome` | 추론 | medium | 게놈 저작 (**스펙마다 1회**, 충돌 시 재시도) | 전 모드 |
| `/api/design/concepts` | 추론 | medium | 스케치당 컨셉 N개 저작 (**진출 디자인마다 1회**) | 전 모드 |
| `/api/verify/render` | 추론 (비전) | **low** | 렌더가 스펙과 맞는지 검사 + 수리 후 재검사 | 전 모드 |
| `/api/analyze/md-review` | 추론 | **high** | MD 페르소나 평가 (Run 당 1회) | 전 모드 |
| `/api/image/generate` `/api/image/edit` | 이미지 모델 | — | 모든 그림 | 전 모드 |
| `/api/model/generate` | 3D 서비스 | — | 최종 픽마다 GLB | 전 모드 |
| `/api/analyze/logo-style` | 추론 (비전) | high | 로고 참조 사진에서 배치 방식 읽기 | 브랜드 설정 시 1회 |

### 1-2. 무과금 엔드포인트

`/api/image/file/*` `/api/model/file/*` (캐시 서빙) · `/api/upload` (디스크 저장) · `/api/shot` (원격 사진 프록시, 자체 과금 없음) · `/api/status` `/api/image/providers` `/api/model/probe` `/api/inference/probe` `/api/research/deep-check` `/api/shot/unlocker-check` (프로브) · `/api/miro/export` (사용자 토큰) · `/api/dev/save-sample`.

---

## 2. 모드별 조사 내역 (S1) · 실측

조사는 **모드가 무엇을 부르는지**로 갈린다. `MODE_SCOPE` 가 정하는 것이 그대로 비용이 된다.

| 조사 레그 | 트렌드 | 시리즈 | 무드보드 |
|---|---:|---:|---:|
| 경쟁사 제품 (`/research/competitors`) | **87회** | — | — |
| 리테일 펄스 (`/research/pulse`) | **31회** | — | — |
| 트렌드 신호 (`/research/trends`) | 20회 | 24회 | — |
| 트렌드 리포트 (하위 질문 N+3개) | **102회** | 78회 | — |
| 시즌 도시에 (`/research/dossier`) | **179회** | **168회** | — |
| 아카이브/문서 읽기 | — | 사진 7장 ×1콜 | PDF 14쪽 ×1콜 |
| **검색 합계** | **419회** | **270회** | **0회** |
| **조사 비용 (추정)** | **~$5–7** | ~$3.5–5 | ~$0.4 |

**읽는 법.** 트렌드가 비싼 이유는 경쟁사(87) + 펄스(31) = 118회를 혼자 더 쓰기 때문이다. 시리즈는 그 둘을 건너뛴다 (`MODE_SCOPE.series.competitor = false`). 무드보드는 외부 조사가 아예 없어 S1 이 사실상 공짜다 — PDF 한 부 읽는 비전 콜 하나가 전부다.

**도시에가 단일 최대 항목이다 (179 / 168회).** 매크로 4개를 각각 깊게 조사하고, 연도별 맥락을 한 번 더 돈다. 다만 캐시 키가 `품목 × 시즌 × 시장 × 라인해시` 라 **브랜드가 달라도 재사용된다** — 같은 품목을 여러 브랜드가 조사하면 이 179회는 한 번만 과금된다.

---

## 3. 기능별 이미지 비용 · 실측

이미지가 전체의 55–80% 다. 어느 기능이 몇 장을 쓰는지가 곧 비용 구조다.

### 3-1. 실측 (detail 엔진 $0.190/장)

| 기능 | 단계 | 생성/편집 | 트렌드 | 시리즈 | 무드보드 |
|---|---|---|---:|---:|---:|
| 기준 스케치 (흑백 선화) | S2 | 생성 | 4장 $0.76 | 3장 $0.57 | 4장 $0.76 |
| **아웃솔 시트** (흑백 선화) | S2 | 생성 | 4장 $0.76 | 3장 $0.57 | 4장 $0.76 |
| 기준 렌더 (히어로) | S3 | 생성 | 1장 $0.19 | 2장 $0.38 | 2장 $0.38 |
| **컨셉 렌더** (스케치당 N–1) | S3 | 편집 | 8장 $1.52 | **9장 $1.71** | 8장 $1.52 |
| 추가 뷰 (내측·3/4) | S3 | 편집 | 8장 $1.52 | 6장 $1.14 | 8장 $1.52 |
| 컬러웨이 | S3 | 편집 | 8장 $1.52 | 6장 $1.14 | 8장 $1.52 |
| 비전 수리 (재생성) | S3 | 편집 | 3장 $0.57 | 1장 $0.19 | 2장 $0.38 |
| **캠페인 컷** (착용·연출) | S4 | 편집 | **12장 $2.28** | 8장 $1.52 | 8장 $1.52 |
| **합계** | | | **48장 $9.12** | **38장 $7.22** | **44장 $8.36** |
| 3D GLB | S5 | — | 3개 ~$0.6–1.2 | 2개 ~$0.4–0.8 | 2개 ~$0.4–0.8 |

*기준 렌더가 1–2장인 이유: 진출 디자인 4개 중 3개가 비전 수리를 거쳐 `regenerated_hq` 로 바뀌었기 때문이다. 수리 3장 + 원본 1장 = 4개 디자인.*

### 3-2. 엔진을 바꾸면 (같은 장수)

| 기능 | 장수(트렌드) | detail $0.190 | fast $0.045 | 절감 |
|---|---:|---:|---:|---:|
| 기준 스케치 | 4 | $0.76 | $0.18 | –$0.58 |
| 아웃솔 시트 | 4 | $0.76 | $0.18 | –$0.58 |
| 추가 뷰 | 8 | $1.52 | $0.36 | –$1.16 |
| 컬러웨이 | 8 | $1.52 | $0.36 | –$1.16 |
| 캠페인 컷 | 12 | $2.28 | $0.54 | –$1.74 |
| **위 다섯만 fast** | 36 | $6.84 | $1.62 | **–$5.22** |
| 기준 렌더 (유지) | 1 | $0.19 | — | — |
| 컨셉 렌더 (유지) | 8 | $1.52 | — | — |
| 비전 수리 (유지) | 3 | $0.57 | — | — |
| **혼합 합계** | 48 | $9.12 | **$3.90** | **–57%** |

**왜 이 다섯인가.** 스케치·아웃솔 시트는 흑백 선화라 detail 의 질감·조명이 쓰일 데가 없다. 추가 뷰와 컬러웨이는 기준 렌더의 **편집**이라 형태를 기준 렌더가 이미 잡아 준다. 캠페인 컷은 분위기가 목적이라 미세 디테일이 판단을 바꾸지 않는다.

**왜 셋은 유지인가.** 기준 렌더는 보드의 얼굴이고 뒤의 모든 편집이 여기서 파생된다. 컨셉 렌더는 `material_shift` 가 곧 질감이라 품질이 내용 그 자체다. 비전 수리는 이미 한 번 실패한 컷이라 더 나은 모델이 필요하다.

---

## 4. 기능별 추론 호출 · 실측

| 기능 | 엔드포인트 | effort | 호출 수 (트렌드/시리즈/무드) | 비용 추정 |
|---|---|---|---|---|
| 영토 계획 | `/design/territories` | high | 1 / 1 / 1 | ~$0.08 each |
| **게놈 저작** | `/design/genome` | medium | **8 / 6 / 8** | ~$0.04 each |
| 컨셉 저작 | `/design/concepts` | medium | 4 / 3 / 4 | ~$0.05 each |
| 비전 검증 | `/verify/render` | **low** | 7 / 4 / 6 | ~$0.02 each |
| MD 리뷰 | `/analyze/md-review` | high | 1 / 1 / 1 | ~$0.10 |
| DNA 읽기 | `/analyze/series` | high | — / 1 / — | ~$0.15 (사진 7장) |
| 문서 읽기 | `/analyze/moodboard` | high | — / — / 1 | ~$0.40 (14쪽) |
| **저작·검증 소계** | | | | **~$0.6–0.9** |

**게놈이 호출 수로는 가장 많다 (스펙마다 1회, 8회).** 다만 `medium` 이고 출력이 스키마 하나라 건당은 싸다. 여기에 다양성 게이트 재시도가 붙으면 최대 3배까지 가는데, 실측에서는 재저작이 몇 건 있었다.

**영토와 MD 만 `high` 다.** 영토는 Run 전체의 설계 공간을 가르는 한 번뿐인 판단이고, MD 는 후보 전체를 놓고 하는 상업 판단이라 깊이가 값을 한다. 나머지는 이미 낮춰져 있다.

---

## 5. 모드별 총계 (실측 + 추정)

| | 트렌드 | 시리즈 | 무드보드 |
|---|---:|---:|---:|
| S1 조사 (검색 419 / 270 / 0) | ~$5–7 | ~$3.5–5 | ~$0.4 |
| S1b·S2·S3 저작·검증 | ~$0.75 | ~$0.65 | ~$0.75 |
| **S2–S4 이미지** | **$9.12** | **$7.22** | **$8.36** |
| S4 MD 리뷰 | ~$0.10 | ~$0.10 | ~$0.10 |
| S5 3D | ~$0.6–1.2 | ~$0.4–0.8 | ~$0.4–0.8 |
| **합계** | **~$15.6–18.2** | **~$11.9–13.8** | **~$10.0–10.4** |
| 소요 시간 | 100분 | 108분 | 103분 |

### 엔진 분리를 적용하면

| | 트렌드 | 시리즈 | 무드보드 |
|---|---:|---:|---:|
| 이미지 (혼합) | $3.90 | $3.28 | $3.62 |
| **합계** | **~$10.4–13.0** | **~$8.0–9.9** | **~$5.3–5.7** |
| 절감 | **–33%** | –30% | **–46%** |

무드보드 절감률이 가장 높은 이유는 조사비가 0 이라 이미지 비중이 80% 로 가장 크기 때문이다.

---

## 6. 자체 개발 손익 (기능 단위)

| 기능 | Run당 장수 | 연 1,000 Run | detail 기준 | fast 기준 | 대체 난도 |
|---|---:|---:|---:|---:|---|
| **기준 스케치** | 4 | 4,000장 | $760 | $180 | **낮음** — 흑백 선화, 스타일 좁음 |
| **아웃솔 시트** | 4 | 4,000장 | $760 | $180 | **낮음** — 같은 선화 문법 |
| 컬러웨이 | 8 | 8,000장 | $1,520 | $360 | 중간 — 색만 바꾸는 편집 |
| 추가 뷰 | 8 | 8,000장 | $1,520 | $360 | 높음 — 형태 유지가 어렵다 |
| 캠페인 컷 | 12 | 12,000장 | $2,280 | $540 | 높음 — 인물·연출 |
| 기준 렌더 | 1 | 1,000장 | $190 | — | 매우 높음 |
| 컨셉 렌더 | 8 | 8,000장 | $1,520 | — | 매우 높음 |

**선화 둘(스케치 + 아웃솔 시트)이 자체 개발 1순위다.** 연 8,000장, detail 기준 $1,520 / fast 기준 $360. LoRA 하나로 커버되고, 실패해도 폴백이 쉽다(선화는 사람이 봐도 맞는지 바로 안다). 다만 **먼저 fast 로 내리는 것이 훨씬 싸고 즉시 된다** — $1,520 → $360 이 설정 한 줄이다. 자체 개발은 그 $360 을 0 으로 만드는 일이라, 우선순위는 fast 전환 뒤다.

컬러웨이는 2순위다. "같은 제품, 색만 다르게"는 오픈 인페인팅이 잘하는 과제고 연 $360(fast 기준)이다.

---

## 7. 요약

1. **이미지가 전부다** — 세 모드 모두 55–80%. 조사는 트렌드에서만 두 번째로 크다.
2. ~~엔진 하나를 다섯 기능만 fast 로 내리면 –57%~~ → **실측으로 반박됨. §8 을 볼 것.** fast 편집은 형태를 유지하지 못한다. 안전한 절감은 –13%(이미지) / –7%(Run).
3. **조사비는 브랜드 수가 아니라 품목×시즌×시장 조합 수** — 도시에 179회가 그 조합당 한 번이다.
4. **자체 개발은 선화 둘만** 값이 있고, 그마저 fast 전환 뒤에 봐야 한다. (§8 로 선화만 fast 가능이 확인됐다)
5. **추론 토큰은 아직 추정** — `usage` 기록이 선행되어야 4·5번이 확정된다.

---

## 8. 엔진 분리를 실제로 찍어 본 결과 — 앞의 권고를 정정한다

§3-2 에서 "다섯 기능을 fast 로 내리면 –57%, 보이는 품질은 그대로" 라고 썼다. **단가만 보고 한 말이었고, 찍어 보니 틀렸다.**

`tools/engine-compare.ts` 로 같은 디자인(SH-26FW-C01)을 두 엔진에 태웠다. 스케치는 프롬프트가 엔진별로 다르므로 각자의 프롬프트로, 편집은 **같은 기준 렌더·같은 프롬프트**에 모델만 바꿔서.

### 8-1. 편집 — 형태가 흐른다 (치명적)

기준 렌더는 샌드 어퍼 + 블루 쿼터, 미드풋 검은 오버레이 스트랩, 조각된 미드솔 스텝, 간격이 뚜렷한 아웃솔 러그, 아치 밑 라임 액센트.

| 요소 | detail 편집 | fast 편집 |
|---|---|---|
| 미드풋 오버레이 스트랩 | 유지 | 대폭 축소 |
| 미드솔 스텝·사이드월 | 유지 | **사라짐 · 매끈한 덩어리** |
| 아웃솔 러그 분절 | 유지 | 뭉개짐 |
| 힐 카운터 구조 | 유지 | 평평해짐 |
| 실루엣 | 동일 | **길고 낮게 변형** |

**detail 은 같은 신발의 다른 색, fast 는 다른 신발이 파란색이다.**

이건 품질 등급 문제가 아니라 **계약 위반**이다. 추가 뷰·컬러웨이·컨셉이 편집인 이유는 "모든 컷이 같은 제품"을 보장하기 위해서고, 보드 카드가 그렇게 적는다. 추가 뷰는 같은 신발을 다른 각도로 보여 주는 것이 전부라 형태 드리프트가 컬러웨이보다 더 나쁘다. 게다가 비전 검증이 형태를 스펙과 대조하므로, 뭉개진 미드솔은 **검사 실패 → 수리 → 재생성**으로 돌아온다. 트렌드 Run 의 수리율이 이미 4개 중 3개였다.

### 8-2. 스케치 — 쓸 만하지만 공짜는 아니다

fast 스케치는 깨끗한 선화로 나온다. 형태·패널·아웃솔 분절이 읽히고 카드에 올려도 문제없다. 다만 `shapePrompt` 의 fast 브랜치에는 소재 표현 규칙이 없다.

```
소재 표현 규칙 언급 · fast 0회 / detail 5회
  (스웨이드 해칭 · 패턴트 하이라이트 · 메쉬 크로스해칭 · 웰트 점선)
```

실제 결과도 그렇다 — detail 스케치는 어퍼 전체에 메쉬 크로스해칭과 미드솔 가장자리 점선 스티치가 들어가는데, fast 는 중앙 패널에만 해칭이 남는다. 렌더는 이 선화에서 소재를 읽으므로, 선화의 소재 정보가 줄면 렌더에도 영향이 갈 수 있다 (이건 이번 테스트로 확인 못 했다).

### 8-3. 속도는 fast 가 3~4배 빠르다

| | fast | detail |
|---|---:|---:|
| 스케치 생성 | 33초 | 137초 |
| 컬러웨이 편집 | 42초 | 135초 |

100분짜리 Run 의 상당 부분이 이미지 대기다. 비용이 아니라 **소요 시간**이 문제일 때는 fast 가 답이 될 수 있다.

### 8-4. 정정된 권고

| 기능 | 앞서 제안 | **정정** | 이유 |
|---|---|---|---|
| 기준 스케치 | fast | **조건부 fast** | 쓸 만하나 소재 해칭 손실. 렌더 영향 미확인 |
| 아웃솔 시트 | fast | **fast 가능** | 프롬프트가 `shapePrompt` 를 안 거쳐 문구가 안 바뀐다. 트레드 도면은 소재 해칭이 무의미 |
| 추가 뷰 | fast | **detail 유지** | 형태 드리프트 = 같은 제품 보장 파기 |
| 컬러웨이 | fast | **detail 유지** | 위와 같음 |
| 캠페인 컷 | fast | **detail 유지** | 이것도 기준 렌더의 편집이다. 다른 신발이 착용컷에 나오면 더 나쁘다 |
| 기준 렌더 · 컨셉 · 수리 | detail | detail | 변경 없음 |

**정정된 절감폭.** 안전하게 옮길 수 있는 것은 아웃솔 시트(4장)와 조건부로 기준 스케치(4장)뿐이다.

| | 장수 | detail | 혼합 | 절감 |
|---|---:|---:|---:|---:|
| 아웃솔 시트만 | 4 | $0.76 | $0.18 | –$0.58 |
| + 기준 스케치 | 8 | $1.52 | $0.36 | –$1.16 |
| **트렌드 Run 이미지 합계** | 48 | **$9.12** | **$7.96** | **–13%** |

**–57% 가 아니라 –13% 다.** Run 전체로는 $15.6–18.2 → $14.4–17.0, 약 –7%.

### 8-5. 그래서 무엇을 해야 하나

비용을 크게 줄이려면 **엔진이 아니라 장수**를 봐야 한다. 형태 보장을 깨지 않고 줄일 수 있는 것은 설정값이다.

| 조치 | 절감 | 잃는 것 |
|---|---:|---|
| `campaignShots` 4 → 2 | –$1.14 | 픽당 연출컷 2장 |
| `colorwayCount` 2 → 1 | –$0.76 | 디자인당 컬러 대안 1개 |
| `viewCount` 3 → 2 | –$0.76 | 3/4 뷰 (내측은 유지) |
| 아웃솔 시트 + 스케치 fast | –$1.16 | 선화 소재 해칭 |
| **합계** | **–$3.82 (–42%)** | 위 넷 |

이쪽이 정직하다. 같은 제품 보장을 지키면서 카드 밀도를 조금 낮추는 거래고, 사용자가 위저드에서 직접 고를 수 있는 값들이다.

### 8-6. 이 테스트의 한계

디자인 하나, 컬러웨이 프롬프트 하나로 본 결과다 (n=1). 다만 차이가 미묘하지 않아 — 미드솔 조각이 통째로 사라지는 수준 — 표본을 늘려도 결론이 뒤집힐 것 같지는 않다. 확정하려면 진출 디자인 4개 × 뷰·컬러웨이로 16장씩 찍어 비전 검증 통과율을 비교하면 된다 (약 $3.8).
