# 주얼리 에이전트에 신발 v2 알고리즘을 옮기는 지시서

이 문서는 `vringon-jewelry` 를 작업하는 사람에게 주는 지시서다. 신발 쪽(`fashion-agent`)이
겪은 문제와 그 해결을 그대로 베끼라는 뜻이 아니다 — **베끼면 안 되는 것이 셋 있고, 그게 제일
중요하다.** 먼저 무엇을 옮기고 무엇을 옮기지 말지부터 정하고 시작하라.

기준: fashion-agent `760b0b8` · vringon-jewelry `HANDOFF.md` 기준 상태.

---

## 0. 지금 주얼리가 서 있는 자리

주얼리는 신발이 v2 이전에 있던 자리에 거의 그대로 있다. 이건 비난이 아니라 좌표다 —
신발이 그 자리에서 무엇을 고쳤는지 알면 순서를 그대로 쓸 수 있다.

| | 신발 v2 | 주얼리 현재 |
|---|---|---|
| 구조 결정의 저자 | LLM 게놈 | `rng` — `packs.ts:73` `generateSpec(rng, tier, itemType, locked)`, `hint` 인자 자체가 없다 |
| 티어를 정하는 값 | 게놈의 `is_new_last` / `is_new_outsole_mold` | `packs.ts:105` `is_new_mold: tier === 'signature' ? rng.chance(0.6) : rng.chance(0.15)` |
| 렌더 검증 | 실제 비전 호출 + 1회 수리 | `pipeline.ts:891` `buildQA()` 가 순수 `rng`, "수리"는 `rng.chance(0.5)` 면 전부 통과 처리 |
| 근거 | `hintApplied`/`hintBlocked` + 출처 등급 | `buildRationale` 이 `rng.pick` 으로 driving signal 을 고름 |
| 다양성 | 5축 게이트 + 반유사 다이제스트 | `recipes.ts` 가 **프롬프트 절만** 돌린다 — 스펙은 그대로 난수 |
| 시장 | 홈 + 참조, 시장별 리테일 로스터 | 없음. KRW 고정, 리테일러가 프롬프트에 하드코딩 |
| MD | `buy`/`buy_if_fixed`/`pass`, 저작 단계에서 봉인 | `pick`/`hold`/`drop`, 선정 후 개입 — **이쪽이 더 낫다. 지키라** |

**주얼리가 이미 더 잘하는 것 둘.** 베끼지 말고 지켜라.
- MD 페르소나의 `priorities[]`(순서가 곧 가중치) + `rejectRules[]` + `checkpoints[]` 설계.
  `brand.ts:13-15` 의 "숫자 가중치는 LLM 페르소나에 안 먹는다"는 판단이 맞다.
- 드롭된 Top 안이 같은 티어 대체가 없으면 **드롭 판정을 단 채 목록에 남는** 규칙
  (`pipeline.ts:712-714`). 신발에는 이게 없다. 조용히 지우면 반대 의견이 사라진다.

---

## 1. 베끼면 안 되는 것 셋

### ① 스케치가 형태를 결정한다는 전제

신발 v2 전체가 이 전제 위에 있다: 검은 실루엣 하나에 토 계열·솔 매스·패널 밀도·스탠스가
전부 들어가고, 그래서 "형태는 스케치, 소재·색은 렌더" 라는 층 분리가 성립한다.

**주얼리는 선이 담는 정보가 훨씬 적다.** 금속 색, 도금, 마감(폴리시/무광/해머드), 스톤 종류,
투명도 — 전부 잉크로 안 보인다. 게다가 **스케일이 안 읽힌다.** 3.8mm 스톤과 5.0mm 스톤은
`packs.ts:123` J-05 의 통과/탈락을 가르는데, 지면 기준선이 없는 선화에서는 구분이 불가능하다.

> **지시:** 게놈의 "그릴 수 있는 축"을 주얼리 어휘로 다시 정의하라. 신발의 5축을 번역하지 말고
> 새로 뽑아라. 후보: **볼륨 계열 · 스톤 배치 토폴로지 · 세팅 형태 · 네거티브 스페이스 밀도 ·
> 파인딩/클로저 형태.** 다양성 게이트(`genome.ts:43` 대응)의 축도 이걸로 바꾼다.
> 스케일 문제는 **치수 기입 도면 규약**이나 **스케일 레퍼런스 오브젝트**로 풀어야 한다 —
> 신발에는 없던 문제니 신발 코드에 답이 없다.

### ② 단일 이미지 3D

신발은 2026-08-13 에 멀티뷰를 **버렸다** (`AGENT-SPEC-v2.md` S8.5). 뷰 간 불일치가 메시를
흐렸고 선정작마다 이미지 3장이 굳었기 때문이다.

**이 결정을 그대로 옮기지 마라.** 신발은 크고 불투명하고 대체로 단일 소재라 3/4 한 컷으로
거의 복원된다. 반지·펜던트는 작고, 반사가 심하고, 관통 구멍이 있고, 뒷면이 앞면과 실제로
다르다. 멀티뷰는 주얼리에서 **더** 정당하다. 주얼리의 현재 4뷰 경로
(`pipeline.ts:762-800`)와 슬롯 순서 `[front, left, back, right]` 를 지켜라.

> 다만 `estimate.ts:57` 이 S5 를 이미지 0장으로 계산하는 건 버그다. 실제로는 top pick 당 4장을
> 만든다. 위저드 비용 표시가 `4 × topN` 만큼 적게 나온다.

### ③ 구조 먼저, CMF 나중

신발은 색·소재를 S3 로 미룬다. 구조가 먼저 서야 비교가 되기 때문이다.

**주얼리는 이 분리가 부분적으로 무너진다.** 로듐 도금 밴드와 골드 버메일 밴드는 같은 디자인의
CMF 변형이 아니라 **다른 가격대의 다른 SKU** 이고, 애초에 다른 생산 라인이다 — 그래서
금속·도금·스톤이 `LineProfile`(`types.ts:137-148`)에 있는 것이다.

> **지시:** 금속 색을 컬러웨이로 다루지 마라. 지금 `colorwayEditPrompt` 의 후보가
> gold/black/bordeaux/ivory/silver 인데 **bordeaux 와 ivory 는 가죽 색이지 금속이 아니다** —
> 신발 잔재다. 주얼리의 컨셉 축은 신발의 (소재/컬러)가 아니라
> **(금속·도금) × (스톤 종류·컷) × (세팅 스타일)** 이어야 한다.
> 라인이 금속을 고정한다면, 컨셉은 그 안에서 도금·스톤·세팅으로 갈린다.

---

## 2. 그대로 옮길 것 — 순서대로

정직성에 직결되는 것부터. 각 항목은 신발에서 실제로 그 순서로 고쳤고, 앞의 것이 뒤의 전제다.

### 1순위 · 난수를 걷어낸다 (반나절씩)

주얼리에서 지금 난수인데 화면에는 계산처럼 보이는 것:

| 무엇 | 어디 | 어떻게 고치나 |
|---|---|---|
| `is_new_mold` | `packs.ts:105` | 게놈이 정하게 하고 룰(J-02)이 검사. **티어의 정의 그 자체가 주사위면 몰드 수·툴링 원가·티어 판정이 전부 그 위에 얹힌다.** |
| `buildQA` | `pipeline.ts:891` | 실제 비전 호출로 교체. 신발 `server/design-api.mjs` `verifyRender` 를 그대로 베껴도 된다 — 스키마의 체크 항목만 주얼리 축으로. |
| "수리" | `pipeline.ts:638-640` | `rng.chance(0.5)` 로 전부 통과 처리하는 코드다. 실패 항목만 짚어 1회 재편집 → 재검사로 교체. |
| `buildModelEval` | `pipeline.ts:882` | 실제로 센 값으로. 신발은 금지 규칙 위반·시그니처 반영·최근접 안과의 스펙 거리·인용 신호의 관측 횟수로 바꿨다. |
| `buildRationale` 의 `rng.pick` | `pipeline.ts:828` | 스펙을 실제로 정한 신호만 인용. 못 정했으면 "아무것도 못 정했다"고 적는다. |

**같이 확인할 것:** 조사가 실패했을 때 샘플 상수로 조용히 대체하는 코드가 있는지.
신발에는 `SIGNALS.shoe` 가 있었고, 그 6개 신호의 출처가 전부 `https://observed.example/…`
— 존재한 적 없는 주소였다. 조사 실패 Run 과 성공 Run 이 화면에서 구분되지 않았다.

### 2순위 · 게놈 저작 (S3/S4)

`recipes.ts` 는 좋은 출발점이지만 **프롬프트 절만 돌린다.** 구조는 여전히 난수다.
신발의 `planTerritories` → `authorGenome` 두 단계를 그대로 가져오되:

- **영토(territory)** 는 그대로 쓸 수 있다. 6개, 상업 앵커 1 + 역트렌드 1, 각 영토가 신호를
  1~3개만 밀고 나머지를 **일부러 버리며 그 이유를 적는** 구조는 품목 무관하다.
- **게놈 스키마**는 ①에서 정한 주얼리 축으로. `spec_sheet` 에 mm·캐럿을 분리하는 규칙은
  그대로 — 이미지 모델은 3.8mm 와 5.0mm 를 구분해 그리지 못한다.
- **파트 블록**은 주얼리 파트로: `shank / head_setting / prongs / gallery / bail_or_post /
  clasp / surface_finish`. 신발의 7파트를 번역하지 말고 주얼리 조립 순서에서 뽑아라.
- **다양성 게이트**는 티어별 최소 상이 축 수. 신발에서 배운 함정: Signature 에 5축 중 3축
  상이를 요구했더니 앞선 안이 8개쯤 쌓인 뒤에는 통과할 자리가 없어 **조용히 룰베이스로
  떨어졌다.** 3회 시도 후에는 겹침을 카드에 적고 저작 안을 채택하는 편이 낫다.

### 3순위 · 출처 4등급제

주얼리 `Signal`(`types.ts:281-298`)에 `source_tiers` 가 없다. 지금 confidence 가 개수 기준이면
소셜 3건이 업계 리포트 1건을 이긴다. 신발의 규칙을 그대로:

```
T1 산업공인 · T2 검증된 시장신호(리테일러 랭킹·재고) · T3 전문매체 · T4 소셜
confidence 는 코드가 등급에서 계산한다 — T1·T2 2곳 = high, T1·T2 1곳 또는 T3 2곳 = medium, 나머지 low
```

**함정:** 빈 배열을 예외로 두지 마라. 신발 첫 구현이 `source_tiers.length` 를 조건에 넣어서,
모델이 `[]` 로 내면 자기가 적은 high 가 살아남았다 — 정확히 이 기능이 막아야 할 것이었다.

### 4순위 · 사람이 끼는 두 지점

- **최종 게이트**: 캠페인 컷·3D 지출 **전에** 멈춘다. 주얼리는 S5 에서 top pick 당 4장을
  만들므로 신발보다 이 게이트의 값어치가 크다.
- **아카이브 승인**: 주얼리에 시리즈 모드가 생기면, 사진에서 읽은 불변 요소는 가설로 두고
  사람이 승인한 것만 스펙을 잠근다.

---

## 3. 입력(S0)부터 바뀌어야 하는 것

여기가 제일 먼저 손대야 하는 곳이다. 뒤 단계는 전부 여기서 정해진 것을 쓴다.

### 시장 모델을 만들어라 — 주얼리에 더 중요하다

주얼리에는 시장 개념이 아예 없다. 그런데 **금 시세·홀마킹 규제·EU REACH 니켈 규정**이
전부 시장별이고, `types.ts:147` 의 `compliance[]` 는 이미 그걸 알고 있는데 프롬프트 문자열로만
쓰인다 — 강제하는 룰이 없다.

신발의 `server/markets.mjs` 를 뼈대로 쓰되 주얼리 것으로 채워라:
- 홈 시장 + 참조 시장 (글로벌은 참조 전용 — 글로벌 정가는 없다)
- 시장별 리테일 로스터. 지금 `research-api.mjs:308-310` 에 하드코딩된 롯데/신세계/현대 ·
  Harrods/Selfridges/Saks · NAP/Mytheresa/Farfetch 를 시장별로 가른다.
- 시장별 통화. `price_krw` 를 이름째 바꾸는 건 크니(신발은 12파일 46곳이라 미뤘다),
  `price_local` + `price_currency` 를 **추가**하고 `price_krw` 는 한국 가격을 실제로 봤을 때만
  채우는 방식이 싸다.
- **검색어 한국어 별칭을 시장으로 게이팅하라.** 신발에서 가장 컸던 발견이다:
  `canonBrand`/`canonTerm` 이 모든 브랜드·품목명을 무조건 한국어로 바꿔 검색해서, 미국 시장을
  조사해도 실제 검색어가 `'페니 로퍼'` 였다. 주얼리도 `BRAND_ALIAS`/`TERM_ALIAS`
  (`research-api.mjs:332-349`)가 같은 구조다.
- **페이지를 실제로 여는 곳의 `Accept-Language`.** 신발에서 이게 계속 `ko-KR` 이라
  "미국 시장을 조사했다"가 네트워크 층에서 거짓이었다. 많은 몰이 이 헤더로 지역몰을 가른다.

### 라인 프로필에 "출발 자산"을 넣어라

신발의 `existingLastReuse` / `existingBottomReuse` 는 Core 티어의 정의다. 주얼리에는
**CAD 모델 레지스트리가 아예 없다** — 몰드 ID 는 `packs.ts:106` 에서
`MLD-2024-${rng.int(3,18)}` 로 지어내고, 위저드의 "22 moulds" 는 하드코딩된 문자열이다.

> **지시:** 신발의 `LAST_LIBRARY`(11개, `family` 필드로 호환성 룰 S-11 을 건다)에 대응하는
> **CAD/왁스 모델 레지스트리**를 만들어라. 이게 없으면 티어가 생산 현실에 묶이지 않는다.
> 반지는 **사이즈가 실제 몰드 치수**인데 현재 원가 모델이 `size_run_count` 를 아예 안 쓴다
> (`packs.ts:166`) — 신발은 사이즈당 몰드가 필요해 `mold_count = size_run_count` 다.
> 주얼리의 사이즈 경제학이 무엇인지부터 정하고 원가에 반영하라.

### 조사 렌즈를 품목별로

신발은 계열(sneaker/dress/…) 렌즈만 있다가, 러닝화·트레일화·레이서가 전부 같은 세 질문을
받는 걸 발견하고 품목별 렌즈를 얹었다. 주얼리는 **18개 품목이 하나의 리테일 지면**을 본다 —
뱅글과 솔리테어가 같은 곳에서 조사된다. `FAMILY_LENS` / `TYPE_LENS` 구조를 그대로 옮겨라.

주얼리에서 반드시 따로 물어야 할 축 (신발의 미드솔·아웃솔에 대응):
- **세팅과 파인딩** — 프롱/베젤/파베/채널, 포스트 게이지, 클래스프 형식
- **스톤 스펙** — 컷·캐럿·클래리티가 가격대별로 어디까지 내려왔는가
- **소셜 언급** — 어느 플랫폼에서, 무엇과 함께, 왜 사는가 (신발과 동일)
- **인접 카테고리** — 주얼리는 시계·아이웨어·헤어 액세서리가 신발의 의류·가방에 해당

---

## 4. 이미지 생성에서 갈리는 것

| | 신발 | 주얼리 |
|---|---|---|
| 필수 뷰 | 측면(+아웃솔 도면 신규) | **매크로가 필수** — `packs.ts:179` `detail: required: true`. 세팅과 마감은 매크로에서만 읽힌다 |
| 조명 | 부드러운 균일광 | **균일광이 금속과 스톤을 죽인다.** 지금 `colorizePrompt`(`aiClient.ts:150`)가 신발과 같은 조명 브리프를 쓴다. 브릴리언스·파이어·신틸레이션·진주 오리엔트 어휘가 프롬프트에 아예 없다 — `PearlSpec` 은 luster/surface/nacre 를 수집하는데 프롬프트에는 맨 단어로만 들어간다 |
| 착용컷 | 전부 발 | **품목마다 신체 부위가 다르다.** `WEAR_SPOT`(`aiClient.ts:180-209`)이 그걸 푼 장치고, 반지가 손목에 끼워지던 버그(`README.md:56-58`)가 그 이유다. 이건 이미 잘 돼 있으니 지켜라 |
| 스케치 변형 | 폐기 → 컨셉 N개 | 같은 방향으로. 단 ③에 따라 컨셉 축이 다르다 |

**청소할 신발 잔재:** `aiClient.ts:51-57`(`TOE_KO`/`HEEL_KO`), `:61-67`(`SHOE_VIEW`),
`:219-235`(샤프트 높이 분기), `sketch.ts:139`. 지금 주얼리 저장소에 그대로 있다.

---

## 5. 보드

신발 보드는 이번에 작업 도구가 됐다. 주얼리도 같은 것이 필요하다:
- 카드 우측 **+** → AI 가 쓴 프롬프트를 보고 고쳐서 재생성, 다른 안과 섞기, 코멘트
- Run 당 SSE 방(`server/board-sync.mjs`)으로 동시 편집 · 이름 달린 코멘트
- 카드마다 **왜 이 소재·색인가** 한 줄. PT 에서 제일 먼저 나오는 질문이다

레인 구성은 주얼리 단계에 맞춰 다시 세워라. 신발은 스케치 레인에 **아웃솔 도면**을 넣었는데,
주얼리의 대응은 아마 **세팅 디테일 도면**(헤드를 위에서 본 것)일 것이다.

---

## 6. 순서 요약

1. **입력** — 시장 모델(홈+참조·통화·별칭 게이팅·Accept-Language) · CAD 모델 레지스트리 ·
   품목별 조사 렌즈. 뒤 단계가 전부 이걸 쓴다.
2. **난수 제거** — `is_new_mold`, `buildQA`, 가짜 수리, `buildModelEval`, `rng.pick` 근거.
   조사 실패 시 샘플 대체가 있으면 같이 제거.
3. **출처 등급제** — 코드가 등급에서 confidence 를 계산. 빈 배열 예외 금지.
4. **게놈 저작** — 영토 → 게놈(주얼리 축 + 주얼리 파트) → 다양성 게이트 → 힌트로 스펙 클램프.
5. **비전 검증 + 1회 수리 + 선정 반영** — 통과 > 미검증 > 실패 순.
6. **사람 게이트** — 지출(캠페인·3D) 전 최종 확인.
7. **컨셉 N개** — 스케치당 여러 디자인. 축은 (금속·도금) × (스톤) × (세팅).
8. **보드** — + 버튼, 동시 편집, 카드마다 '왜'.

각 단계마다 신발에서 배운 것: **"했다"와 "그 결과가 다음 단계를 바꿨다"는 다르다.**
검증을 하고도 선정에 안 쓰면 검증이 아니고, 브랜드를 설정받고도 캐시 키에 안 넣으면
브랜드가 결과를 안 바꾼다. 붙일 때마다 "이게 실제로 무엇을 바꾸는가"를 한 번 확인하라.

---

## 7. 참고 파일

**신발 쪽 (베낄 것)**
```
server/markets.mjs              시장 모델 · 로스터 · 검색 언어 · user_location
server/category-templates.mjs   계열/품목별 조사 렌즈
server/design-api.mjs           영토 계획 · 게놈 저작(파트 포함) · 컨셉 저작 · 비전 검증
src/core/genome.ts              다양성 게이트 · genomeToHint · brandSummaryOf(MD 봉인 이유 주석)
src/core/pipeline.ts            S1~S5 전체 · 게이트 셋 · 수리 루프 · 선정 정렬
src/core/aiClient.ts            프롬프트 층 분리 · partsClause · outsoleSketchPrompt
src/core/boardModel.ts          레인 구성 · 근거 엣지
src/ui/Board.tsx                RemixPanel(+ 버튼) · 코멘트 · 동시 편집
server/board-sync.mjs           SSE 방
tools/run-sample.ts             헤드리스 샘플 생성 (게이트 자동 통과)
README.md                       단계 → 파일 지도
```

**주얼리 쪽 (고칠 것)**
```
src/core/packs.ts:73,105,891    난수 스펙 · 난수 티어
src/core/pipeline.ts:638,828,882 가짜 수리 · 난수 근거 · 난수 평가
src/core/recipes.ts             프롬프트만 도는 다양성 → 게놈으로 승격
src/core/types.ts:137,281       LineProfile(시장 없음) · Signal(등급 없음)
server/research-api.mjs:308,332 하드코딩 리테일러 · 무조건 한국어 별칭
src/core/aiClient.ts:51,61,219  신발 잔재
src/core/estimate.ts:57         S5 이미지 0장으로 계산 (실제 4×topN)
```
