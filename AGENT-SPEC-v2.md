# VRINGON Design Agent v2 · 최종 알고리즘 개선 지시서

작성일 2026-08-13 · 상태: **확정 · Phase 1 개발 중** (기본 모드 = 라이트 · 3D는 단일 이미지 방식, 2026-08-13 사용자 결정)

이 문서는 실측으로 확인된 문제(디자인 수렴·룰 기반 저작·가짜 근거)를 해소하는
최종 구조와, 단계별 API 연동을 확정하기 위한 판단용 지시서다.

---

## 0. 해결하는 문제 (실측 근거)

| # | 실측된 문제 | v2에서의 해법 |
|---|---|---|
| 1 | 스케치 프롬프트 어휘 61% 동일, 차이가 선화로 못 그리는 소재에 몰림 | 구조 우선/CMF 후행 분리 (S6→S7) |
| 2 | 구조 결정(토·힐·패널)이 `rng.pick()` — LLM이 디자인을 저작하지 않음 | S4 Design Genome = LLM 저작, 룰은 검증만 |
| 3 | 조사→스펙이 정규식 매칭 | S2 아톰 추출 = LLM, 근거 분리 강제 |
| 4 | QA가 rng 시뮬레이션 (가짜) | S8 실제 비전 검증 4축, rng QA 폐기 |
| 5 | 강조 라벨과 실제 스펙 모순 | 단일 진실 원천: 프롬프트는 게놈에서만 파생 |
| 6 | 브랜드가 CMF 표면층에만 개입 → 브랜드 간 결과 수렴 | 브랜드 렌즈를 조사 질문(S1B)·해석(S2B)·영토(S3)에 주입 |
| 7 | MD가 생성 단계에 영향 (사용자 요구 위반 가능성) | Merchandising Brief(기획 사실)만 S0, 주관 취향은 S9 봉인 |
| 8 | 출처 등급 없는 열린 웹 조사 | 출처 4등급제, 트랙별 차등 |

---

## 1. 모델 라우팅 (역할 기반)

UI·저장물에는 벤더명을 노출하지 않고 역할명만 쓴다(기존 관례 유지).
설정 파일(.env)의 키 유무로 모드가 자동 결정된다.

### 1.1 역할 정의와 벤더 배정

| 역할 | 기본(표준 모드) | 라이트 모드 | 딥 모드 | API |
|---|---|---|---|---|
| deep-researcher (조사) | **GPT** gpt-5 | GPT | GPT | OpenAI Responses API + `web_search` 도구, `reasoning.effort: high`, strict json_schema. **현행 research-api.mjs 재사용** |
| counter-verifier (반대근거·검증) | **Gemini** flash 계열 | GPT 자기적대 패스 | **Claude** (web_fetch로 원문 대조) | Gemini: generateContent (구조화 출력 미지원 → 검증 서술만 받고 JSON화는 GPT 후처리). Claude: Messages API + web_search/web_fetch 도구. **Claude는 신규 통합(Phase 2)** |
| schema-extractor (아톰·게놈 JSON화) | GPT | GPT | GPT | OpenAI strict structured output. 현행 `ask()` 패턴 재사용 |
| vision-reader (아카이브·로고·문서 리딩) | GPT | GPT | GPT | OpenAI Responses `input_image`/`input_file`. **현행 upload-api.mjs 재사용 (조던·MICAM 실증 완료)** |
| sketch-artist (실루엣·스케치 대량) | **Gemini** 이미지 | GPT gpt-image-1.5 | Gemini | **현행 gemini-api.mjs의 geminiGenerate/geminiEdit 재사용** (GEMINI_FALLBACK_ONLY 해제, 역할 라우팅으로 전환) |
| render-artist (최종 렌더·편집) | **GPT** gpt-image-1.5/2 | GPT | GPT | OpenAI images.generations/edits. **로고 합성·편집 파이프라인이 GPT 기반이므로 유지** |
| render-verifier (S8 4축 검증) | **Gemini** 비전 | GPT 비전 | Gemini+Claude 이중 | 생성 벤더와 **다른** 벤더가 검증 (교차 검증 원칙) |
| md-reviewer (S9) | GPT | GPT | GPT | 현행 reviewAsMd 재사용, pairwise는 비전 입력 |

### 1.2 원칙

1. **생성과 검증은 다른 벤더.** 같은 모델이 자기 결과를 검증하면 자기 습관을 통과시킨다.
   라이트 모드에서만 예외적으로 동일 벤더 자기적대 패스 허용.
2. **모델 ID는 라우터 한 곳에만.** 각 단계 코드는 역할명으로만 호출한다.
3. **Gemini 이미지의 용도는 "많이·싸게"** (실루엣 24장, 스케치 변형),
   **GPT 이미지의 용도는 "정확히"** (게놈 충실 렌더, edit 기반 뷰·컬러웨이·로고 합성).
4. Claude는 딥 모드 전용. 키가 없으면 해당 역할이 Gemini→GPT 순으로 강등되고,
   강등 사실을 실행 로그에 남긴다 (조용한 대체 금지).

### 1.3 비용 모드 (사용자 다이얼 · UI 노출)

| 모드 | 조사 | 검증 | 이미지 | 예상 콜 수 | 예상 비용/실행 |
|---|---|---|---|---|---|
| **라이트 (기본 · 확정)** | GPT (현행 수준) + S1B(경량)·S2B·S2.5 | GPT 자기적대 (로그 명시) | GPT only | 50~80 | $5~9 |
| 표준 (다이얼 선택) | GPT + S1B/S1C 전체 | Gemini | Gemini(대량)+GPT(정밀) | 120~180 | $12~25 |
| 딥 (다이얼 선택) | GPT + 전 트랙 | Claude+Gemini | 표준+3뷰+2라운드 | 250~400 | $30~55 |

라이트가 기본이어도 v2의 구조 전환(게놈 저작·영토·게이트·단일 진실 원천·rng QA 폐기)은
전부 포함된다. 모드는 **콜 수와 검증 벤더의 차이**이지 알고리즘 구조의 차이가 아니다.
Gemini·Claude 활용은 사용자가 다이얼을 올릴 때만 켜진다.

---

## 2. 파이프라인 상세 (S0~S10)

### S0 · 브랜드 스냅샷 (버전 고정)

**API: vision-reader (GPT).** 신규 조사 없음.

- [필수] 아카이브 사진 리딩 → **DNA 가설** (`hypothesis / confidence / evidence_product_ids / human_approved:false`).
  사람이 승인하기 전에는 불변요소로 쓰지 않는다. 현행 시리즈 리딩 코드 승격.
- [필수] **축별 보존 강도 5축** (silhouette/sole/upper_topology/closure/cmf, 0~1).
  단일 brand_distance 폐기.
- [필수] 금지요소 · 시즌 전략 브리프(간이 폼) · 생산 자산(보유 라스트·몰드).
- [권장] 시그니처 계층 A~D · **Merchandising Brief**
  (SKU 역할 수, 가격 사다리, 자기잠식 제한, 출시 시점 — *기획 사실*, MD 취향 아님).
- [선택] 판매 이력·구색 DB·착화 DNA. 없으면 관련 판단에
  "데이터 부족으로 생략"을 결과물에 명시 (조용한 기본값 금지).
- [봉인] MD 주관 취향·선택 이력·과거 실패(구조화된 failure_type 포함) → S9 전까지 읽기 불가.
- UI: **브랜드 렌즈 강도 게이지** — 입력이 빈약하면
  "결과가 시장 평균에 수렴합니다" 경고를 실행 전에 표시.

### S1 · 조사 (3갈래 + 검증 패스)

**API: deep-researcher (GPT Responses + web_search), counter-verifier (Gemini/Claude).**

- **S1A 시장 공유 조사** — 캐시 키: 품목+시즌+시장+가격대 (브랜드 무관, 브랜드 간 공유로 비용 절감).
  트랙: 상업 수요 / 텍스트 디자인 문법 / 소재·공법 / 생산 실현성.
  현행 researchCompetitors·retailPulse·trends·dossier를 트랙 구조로 재편 (호출 로직 재사용).
- **S1B 브랜드 델타 조사** — 캐시 키: 시장스냅샷id+브랜드스냅샷id.
  실경쟁군 / 라인업 빈 공간 / 브랜드 과사용 요소 / 자기잠식 후보.
  브랜드 스냅샷이 프롬프트 조건으로 들어간다.
- **S1C 비주얼 문법 조사 (한정판)** — 수집된 경쟁사·베스트셀러 **사진**을
  vision-reader에 넣어 실루엣·패널·솔·클로저 특징을 구조 추출 → Visual Trend Atom.
  클러스터링 인프라 없음. 현행 shot 캐시 재사용.
- **검증 패스** — 각 트랙 결과의 핵심 클레임에 대해 counter-verifier가
  반대 근거·과장·출처 충돌을 지적. 딥 모드에서는 Claude가 web_fetch로 원문 대조.
- **출처 4등급제** — evidence마다 `source_tier` 기록.
  T1 산업 공인(WWD·BoF·FDRA·MICAM·Lineapelle·Pantone) /
  T2 검증된 시장 신호(백화점·공식몰·Vogue Runway·StockX·무신사·KREAM) /
  T3 전문 매체 / T4 소셜·포럼.
  high confidence는 T1~T2 상이 출처 2곳 이상.
  약신호·역트렌드 트랙만 T3~T4 허용(등급 표기+confidence 상한+무효화 조건 필수).
  유료 기관(WGSN 등) 2차 인용은 "2차 인용" 표기, T1 대우 금지.
- **카테고리 조사 템플릿 (신규 · 이번 검토에서 확인된 공백)** —
  현행은 품목명(typeKo)과 라인 프로필 주입까지만 품목을 반영하고,
  조사 렌즈·리테일 대상·도시에 세그먼트는 전 카테고리 공통이다. v2에서 계열별 특화:
  - 렌즈 특화: 힐 = 피치·토박스·힐 플레어·착화 안정 / 러닝 = 스택·드롭·폼·플레이트 /
    부츠 = 샤프트·방수·웰트·아이젠 호환 / 샌들 = 풋베드·스트랩 구조 (계열당 렌즈 셋 별도 정의)
  - 리테일 펄스 대상 지면 계열별 차등 (러닝 = 러닝 전문점·스포츠 편집,
    힐·드레스 = 백화점 구두 층, 부츠 = 아웃도어 편집 병행)
  - 도시에 키아이템 세그먼트 로직 품목별 (펌프스에 kids 세그먼트 강제 금지 등)
  - S2 아톰 커버리지 축의 가중도 계열별 (러닝은 바텀 유닛 축 필수, 드레스는 공법·라스트 축 필수)
- **티어별 Research Barrier**:
  Core = S1A 완료 시 생성 시작 가능 /
  Push = +소재·공법 혁신, 화이트스페이스 /
  Signature = +문화·약신호, 역트렌드, 비상업화 조형.
  → 실행은 자연히 파이프라인: Core 먼저 흘러가고 Signature가 가장 늦게, 가장 다르게.

### S2 · Trend Atom + 브랜드 관련성

**API: schema-extractor (GPT).**

- **S2A 시장 공통 아톰** — 아톰 = 설계 축 1 + 방향 1. 형용사 단독 금지.
  관측/해석/예측/반대근거 분리. 수치는 범위 그대로 (30mm 붕괴 금지, 영토별 분산 배정).
  개수 고정 없음 — 필수 설계 축 커버리지(라스트·비례·어퍼 토폴로지·심·클로저·솔·소재·CMF·
  상업성·생산성·문화·역트렌드)와 신규 정보 증가율로 중단. 좁은 브리프 기본 20~40.
- **S2B 브랜드 관련성 매핑** — 아톰마다 이 브랜드에서의 관련성/신규성/위험성.
  브랜드 데이터 없으면 unknown (추정 금지).

### S2.5 · Season Translation

**API: schema-extractor (GPT).**

아톰별: 현재 성숙도 → 목표 시즌 예상 성숙도 · 포화 위험 · 리드타임 적합 ·
티어별 적용 수준(core/push/signature).
**가드: 판정마다 evidence_id 연결 필수, 근거 없으면 unknown, invalidation_condition 필수.**

### S3 · Concept Territory

**API: deep-researcher 아님 — schema-extractor (GPT), 검색 없음.**

브랜드 조건부 기회 지도 → 영토 5~8 (Merchandising Brief의 SKU 역할 수가 개수 결정).
영토마다: 소비자 역할 / 쓸 아톰 / **버릴 주류 신호** / 허용 티어 / 출발 자산(보유 라스트·몰드) /
축별 허용 브랜드 거리. 품목 인지형 — 품목 정체성과 충돌하는 영토는 생성 금지.

### S4 · Design Genome

**API: schema-extractor (GPT 기본, 딥 모드에서 GPT/Claude/Gemini 순환 배정).**

- 영토당 2~3, **영토마다 독립 호출** (한 호출 다건 생성 금지 — 앵커링 방지).
  뒤 호출에는 기채택 게놈의 반유사 다이제스트를 조건으로.
- Hero Mutation 정확히 1 + Supporting 2~4 + Preserve/Change/Free/Forbidden 셋.
- **스키마에는 그릴 수 있는 축만** (실루엣 계열·스탠스·솔 매스·패널 밀도·클로저 형식·토 패밀리).
  mm·비율은 스펙시트 전용 필드로 분리 (이미지 지시에 사용 금지).
- **단일 진실 원천: 이후 모든 프롬프트는 게놈 필드에서 기계 조립.** 아톰·조합 라벨 직접 참조 금지.
- source_atom_ids + 채택/거부 기록 (추적 체인).

### S5 · 게이트 (API 비용 0 · 순수 코드)

- ① 하드 실현성 (생산·가격 — 결정적).
- ② 티어별 다양성: Core 구조축 ≥1 + CMF-only 금지 / Push ≥2 + Hero 가시성 /
  Signature ≥3 + 아카이브 거리 + 공유 기준 이미지 금지.
- ③ 복제 위험 1차 (게놈 수준 — 아카이브·경쟁사 스펙과 과도 일치).
- **품질 판단 금지.** 실패 시 충돌 축만 재롤, 3회 실패 → 새 Hero.

### S6 · 스케치 2단

**API: sketch-artist (Gemini 이미지 — 대량·저가), 정합성 검사는 render-verifier.**

- ① 채운 흑백 실루엣 (게놈의 형태 필드만 사용, 동일 카메라·축척·스타일 강제)
  → 코드+비전 중복 제거.
- ② 구조 스케치 — 티어별 뷰:
  Core lateral / Push lateral+top / Signature lateral+medial+top.
  lateral 먼저 생성 후 나머지 뷰는 edit로 파생 (신규 생성보다 일관성 우위).
- 정합성: **실제 비전 검사 1회 + 수리 1회 + 실패 시 정직한 불일치 플래그.**
  무한 수리 금지. "보장"이라는 말 금지.
- 각 스케치에 why-this-sketch = 게놈 서사 부착 (보드·리포트 공용).

### S7 · CMF

**API: schema-extractor (GPT) + 코드.**

구조 확정 후에만. 후보당 컬러웨이 2~3 (전원 6개 금지).
파트별 역할(Anchor/Secondary/Accent)·면적 비율·명도/채도/광택 대비.
Material-led 영토만 예외: 소재가 S4에서 선결정.
소재 DB에 없으면 substitute_required 표기 (확정 금지).

### S8 · 렌더 + 4축 검증

**API: render-artist (GPT images.edits — 구조 스케치 기반), render-verifier (Gemini 비전).**

- 렌더 = 구조 스케치의 edit. "형태 유지, 색·질감만" 지시.
  브랜드 마크는 **학습된 배치 규칙**(현행 로고 스타일 리딩 재사용)으로 형태 묘사,
  참조 사진 없으면 파일 합성 폴백 (현행 유지).
- 검증 4축 (생성과 다른 벤더):
  ① Genome Fidelity — 게놈↔렌더 구조 일치 (**rng QA 완전 폐기**)
  ② 뷰 간 일관성 — 검사+플래그
  ③ **Logo-hidden Brand Fit** — 로고 가리고 구조만으로 브랜드가 읽히는가,
     시장 평균처럼 보이지 않는가
  ④ 중복·복제 위험 — 후보 간 + 아카이브·경쟁사 이미지 대비
- 수리: 불일치 컷만 1회 재편집. 실패는 실패로 표기.

### S8.5 · 3D 쇼룸 (단일 이미지 방식 · 2026-08-13 변경)

**API: Tripo image_to_model.**

- 기존 직교 4뷰 턴어라운드(멀티뷰) 방식 폐기. 최종 선정작의 기준 렌더 **한 장**으로 생성.
- 근거: 턴어라운드 3컷의 뷰 간 불일치가 3D 형상을 오히려 흐리는 사례 관측,
  비용 절감(선정작당 이미지 3장 제거), Tripo 단일 이미지 품질 자체 개선.
- 견적에서 S5 턴어라운드 항목 삭제 (선정작당 이미지 0장, Tripo 호출 1건).

### S9 · 선정 (MD 봉인 해제)

**API: md-reviewer (GPT, 비전 입력), 순서·필터는 코드.**

- ① 하드 필터 (Merchandising Brief 기준 — 결정적 코드)
- ② 블라인드 평가 — 이미지+핵심 스펙만, 서사 숨김, 후보 순서 무작위
- ③ Pairwise 토너먼트 (~20비교) — A/B/둘다/둘다아님 + reason code + 확신도
- ④ 포트폴리오 3종 (Commercial/Balanced/Directional) + 군집 검사
  (같은 실루엣·솔·클로저·영토 편중 검출)
- ⑤ **인간 게이트 — 최종 결정은 사람.**
  buy_if_fixed → 게놈 mutation으로 변환 → 해당 축만 재생성 → 재비교.
- **2라운드**: S9의 선택·기각 이유를 제약으로 S4만 재실행 (딥 모드 기본, 표준 모드 옵션).
- **디자이너 개입 지점**: 렌더 전(S6 직전) 게놈 편집 UI.
  "이 안에서 클로저만 바꿔" 수준의 필드 수정 후 해당 부분만 재생성.

### S10 · 로그 (학습은 보류)

선호(pairwise) / 발주 / 판매를 **분리 저장**. 세 신호를 한 점수로 합치지 않는다.
데이터가 쌓이기 전 학습 모델 없음. 스키마만 확정.

---

## 3. 추적 체인 (모든 최종 디자인 필수)

```
출처(source_tier·날짜) → 아톰 → 시즌 해석 → 브랜드 관련성
  → 영토 → 게놈(채택/거부 포함) → 스케치·렌더 구현 요소 → MD 선택 이유
```

체인이 끊긴 디자인은 결과물에 "근거 미연결" 표시. 조용히 통과 금지.

---

## 4. 실행별 성적표 (매 실행 자동 산출)

| 지표 | 산출 방법 | 목표 |
|---|---|---|
| 구조 다양성 점수 | 채택 후보 간 5축 상이도 평균 (코드) | 티어별 문턱 이상 |
| 로고 숨김 브랜드 판별 | render-verifier 1콜 — 로고 가린 렌더에서 브랜드 추정 | 시장 평균 판정 0건 |
| 체인 완결률 | 코드 검사 | 100% |
| 검증 통과율 | S8 4축 결과 집계 | 추이 관찰 (목표치는 데이터 후 설정) |

"다양해졌다/브랜드다워졌다"를 매 실행 숫자로 찍는다. 눈 논쟁 금지.

---

## 5. 규칙 (전체 23)

기존 1~13 (요약): 필수 조사 전 생성 금지 · 실패를 기본값으로 대체 금지 ·
한 호출 다건 생성 금지 · 전 신호 결합 금지 · Hero 1개 · CMF 차이는 다양성 아님 ·
Push/Signature 공유 기준 이미지 금지 · 구조 확정 전 렌더 금지 · LLM 단독 최종 순위 금지 ·
구색 최적화 · **프롬프트는 최종 게놈에서만** · **그릴 수 있는 축만 게놈에** ·
**시뮬레이션 QA를 게이트로 쓰지 않음**.

```
14. 조사 질문은 브랜드 조건부다. 시장 사실만 공유하고 기회는 공유하지 않는다.
15. 티어마다 조사 완료 조건이 다르다. Signature는 약신호·역트렌드 없이 시작하지 않는다.
16. 아카이브 리딩 결과는 가설이다. 사람이 승인하기 전에는 불변요소로 확정하지 않는다.
17. Season Translation의 모든 판정은 evidence_id를 달고, 근거 없으면 unknown으로 둔다.
18. 다양성 게이트는 품질을 판단하지 않는다. 품질 판단은 S9 전에 존재하지 않는다.
19. 생성 벤더와 검증 벤더는 다르다. 라이트 모드의 자기적대 패스만 예외이며 로그에 남긴다.
20. 벤더 강등(키 없음·장애)은 조용히 일어나지 않는다. 실행 로그에 명시한다.
21. 반복 라운드는 1회 이상 제공한다. 선택·기각 이유는 다음 라운드의 제약이 된다.
22. 브랜드 자산(로고·아카이브)은 해당 브랜드 소유자만 등록한다.
23. 블라인드 테스트(다수 브랜드·복수 시즌) 전에는 어떤 문서에도 "보장"이라 쓰지 않는다.
```

---

## 6. 기존 자산 재사용 맵 (버리는 것 최소화)

| 기존 자산 (검증 완료) | v2에서의 자리 |
|---|---|
| research-api.mjs (브랜드별 분할·다단계 조사·캐시) | S1A 골격 그대로, 트랙 구조로 재편 |
| upload-api.mjs (아카이브·문서·로고 리딩) | S0 vision-reader 그대로 |
| 로고 스타일 학습 → 형태 묘사 프롬프트 | S8 렌더 그대로 |
| shot 프록시·사진 동결·로고 URL 필터 | S1C 입력 그대로 |
| gemini-api.mjs (generate/edit/probe) | sketch-artist·render-verifier로 역할 전환 |
| 티어=툴링 룰(S-01~15)·원가 모델 | S5 하드 실현성 그대로 |
| 보드·리포트·도시에 렌더러 | 서사 소스만 게놈 체인으로 교체 |
| MD 리뷰(reviewAsMd) | S9 골격 그대로, 블라인드·pairwise 추가 |
| **폐기**: generateSpec의 rng 구조 결정, 정규식 신호 파싱, rng QA, 단일 brand_distance | — |

---

## 7. 단계 도입 계획

| Phase | 내용 | 신규 API 통합 | 예상 기간 |
|---|---|---|---|
| **1** | S0 계층화·가설 승인·보존 강도 5축 / MD 분리 / S2B·S2.5 / 게놈 저작 전환(S4) + 코드 게이트(S5) / 단일 진실 원천 / rng QA 폐기 / 비용 모드 다이얼 | 없음 (전부 GPT 현행 키) | 코드 위주 |
| **2** | S1B·S1C / Gemini 역할 라우팅(스케치·검증) / 티어 파이프라이닝 / 티어별 뷰 / S8 4축 / 블라인드+pairwise / 포트폴리오 / 성적표 / **서버 오케스트레이션+체크포인트 (선행 조건)** | Gemini 라우팅 활성화 | 파이프라인 재편 |
| **3** | Claude 검증 통합(딥 모드) / 2라운드 기본화 / 디자이너 게놈 편집 UI / 멀티이미지 참조 렌더(API 지원 확인 후) / S10 학습 | Claude Messages API | 보류 항목 |

각 Phase 종료 시 성적표 지표로 전/후 비교를 남긴다.

---

## 8. 확정 사항 (2026-08-13)

1. **기본 모드 = 라이트** (GPT 단독, $5~9/실행). 표준·딥은 UI 다이얼로 사용자 선택.
2. Claude 통합은 Phase 3 (딥 모드 전용). Gemini 이미지·검증은 표준 모드부터.
3. 이에 따라 Phase 1은 신규 API 통합 0건 — 현행 OpenAI 키만으로 전체 구조 전환 가능.

미결(블로커 아님 · 해당 Phase에서 결정):
- 멀티이미지 참조 렌더 여부 — Phase 3, API 지원 확인 후
- 성적표 목표치 — 데이터 축적 후 설정 (§4)
- S10 학습 모델 — 판매 데이터 확보 전 착수 금지 (규칙 위반 아님, 설계 의도)
